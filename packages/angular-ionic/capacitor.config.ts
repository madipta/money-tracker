import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'angular-ionic',
  webDir: '../../dist/packages/angular-ionic',
  bundledWebRuntime: false,
};

export default config;
