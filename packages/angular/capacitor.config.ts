import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'angular',
  webDir: '../../dist/packages/angular',
  bundledWebRuntime: false,
};

export default config;
